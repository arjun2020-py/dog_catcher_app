import 'package:flutter/material.dart';

class ScreenReportDog extends StatelessWidget {
  const ScreenReportDog({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('report dog'),
      ),
    );
  }
}